# คู่มือติดตั้งส่วนใหม่ — รูปภาพ · ชำระเงิน · สัญญา

ทำตามลำดับ ข้อ 1–2 ทำได้ทันที · ข้อ 3 รอ merchant account

---

## สรุปสิ่งที่เพิ่มเข้ามา

| ฟีเจอร์ | สถานะโค้ด | ต้องทำอะไรต่อ |
|---|---|---|
| ประเภท "ห้องแถว" | ✅ พร้อม | — |
| หน้านโยบาย/ข้อกำหนด (`/privacy`, `/terms`) | ✅ พร้อม | — |
| อัปโหลดรูปจริง | ✅ พร้อม | รัน `storage.sql` |
| ระบบจอง + มัดจำ | ✅ พร้อม | รัน `payments.sql` |
| สัญญาดิจิทัล + ลงนาม | ✅ พร้อม | รัน `payments.sql` |
| ชำระเงินจริง (PromptPay/บัตร) | ✅ โค้ดพร้อม | สมัคร Omise + deploy function |

---

## 1. รัน SQL (จำเป็น — ทำก่อน)

Supabase → SQL Editor → รันทีละไฟล์:

1. **`supabase/storage.sql`** — สร้าง bucket เก็บรูป + สิทธิ์
2. **`supabase/payments.sql`** — ตาราง bookings / payments / escrow + ต่อยอด contracts

รันครั้งเดียว ถ้ารันซ้ำก็ไม่พัง (เขียนแบบ idempotent ไว้แล้ว)

> ตรวจว่าสำเร็จ: Supabase → Table Editor ต้องเห็นตาราง `bookings`, `payments`, `escrow_ledger`
> และ Storage ต้องเห็น bucket `listing-photos`

## 2. ทดสอบ (ยังไม่ต้องมี Omise)

หลังรัน SQL และ deploy เว็บแล้ว:

- **อัปโหลดรูป** — แดชบอร์ด → ประกาศของฉัน → ลงประกาศใหม่ → ขั้นที่ 2 กดเลือกไฟล์
  รูปขึ้น preview พร้อมป้าย "ปก" บนรูปแรก · ลงประกาศแล้วรูปจะโชว์จริงบนการ์ด
- **จอง** — เปิดหน้าประกาศใดก็ได้ → กด "🔒 จองและวางมัดจำ" → ไปดูที่แท็บ "การจอง / มัดจำ"
- **เจ้าของตอบรับ** — ล็อกอินด้วยบัญชีเจ้าของประกาศนั้น → กด "ตอบรับ"
- **สัญญา** — หลังชำระเงินแล้ว เจ้าของกด "สร้างสัญญา" → ทั้งสองฝ่ายเข้าแท็บ "สัญญาเช่า" → พิมพ์ชื่อเต็ม → ลงนาม
  เมื่อลงนามครบ 2 ฝ่าย สถานะเปลี่ยนเป็น "มีผลบังคับใช้" อัตโนมัติ

> ปุ่ม "ชำระมัดจำ" จะแจ้งว่ายังไม่ได้เชื่อม Omise จนกว่าจะทำข้อ 3

## 3. เปิดชำระเงินจริงด้วย Omise

### 3.1 สมัคร merchant account
1. https://www.omise.co → Sign up
2. ยื่นเอกสาร: หนังสือรับรองบริษัท / ทะเบียนพาณิชย์, บัตรประชาชนกรรมการ, บัญชีธนาคารนิติบุคคล
3. รออนุมัติ (ปกติ 3–7 วันทำการ)
4. เปิดใช้ช่องทาง **PromptPay** และ **Credit Card** ใน Dashboard

> ระหว่างรออนุมัติ ใช้ **Test keys** (`pkey_test_...` / `skey_test_...`) ทดสอบได้เลย

### 3.2 ตั้ง secret แล้ว deploy
```bash
cd ~/Desktop/rentbegin
supabase secrets set OMISE_SECRET_KEY=skey_test_xxx OMISE_PUBLIC_KEY=pkey_test_xxx
supabase functions deploy omise-charge
supabase functions deploy omise-webhook --no-verify-jwt
```

### 3.3 ตั้ง Webhook
Omise Dashboard → **Webhooks** → Add endpoint:
```
https://hrgzmbdsuarsfmhrzmhk.supabase.co/functions/v1/omise-webhook
```
จำเป็นสำหรับ PromptPay เพราะผู้ใช้สแกนจ่ายทีหลัง Omise จะยิงมาบอกว่าจ่ายแล้ว
ระบบจะอัปเดตสถานะ + ออกใบเสร็จอัตโนมัติ

### 3.4 ทดสอบ
บัตรทดสอบของ Omise: `4242 4242 4242 4242` วันหมดอายุอนาคต CVV อะไรก็ได้
PromptPay โหมด test จะได้ QR ปลอมที่กดยืนยันได้จาก Omise Dashboard

---

## กระแสเงิน (escrow) ทำงานอย่างไร

```
ผู้เช่าจ่าย → เงินเข้าบัญชี Omise ของคุณ
           → payments.status = 'held'  (พักเงิน)
           → escrow_ledger บันทึก action='hold'
           → ออกใบเสร็จให้เจ้าของอัตโนมัติ

เมื่อเข้าอยู่จริง / ไม่มีข้อพิพาท
           → แอดมินกดปล่อยเงิน (action='release')
           → โอนเข้าบัญชีเจ้าของตามรอบ Omise หักค่าธรรมเนียม 1%
```

**ค่าธรรมเนียม:** ระบบคิด 1% ของค่าเช่าล่วงหน้า (แก้ได้ที่ `calcBooking` ใน `src/api/client.js`
และฟังก์ชัน `calc_booking_total` ใน `payments.sql`)

> ⚠️ การปล่อยเงินออกจาก escrow ยังเป็นขั้นตอน manual (แอดมินสั่งโอนใน Omise Dashboard)
> ถ้าต้องการอัตโนมัติต้องใช้ Omise Transfers/Recipients API ซึ่งต้องยืนยันบัญชีผู้รับเพิ่ม

---

## 4. ยื่น Facebook App Review (เปิดให้คนทั่วไปล็อกอิน)

ตอนนี้มีหน้านโยบายแล้ว ใช้ค่าเหล่านี้กรอกใน Meta Dashboard → การตั้งค่าแอป → พื้นฐาน:

- **URL นโยบายความเป็นส่วนตัว**: `https://rentbegin.com/privacy`
- **URL ข้อกำหนดของบริการ**: `https://rentbegin.com/terms`
- **URL คำแนะนำการลบข้อมูล**: `https://rentbegin.com/privacy`
- **หมวดหมู่**: Business and Pages หรือ Lifestyle
- **ไอคอนแอป**: ต้องมีรูป 1024×1024 (ยังไม่มี — ต้องทำโลโก้)

จากนั้น → การตรวจสอบแอป → ส่งขอสิทธิ์ `email` + `public_profile`

## 5. ข้อควรระวังด้านกฎหมาย

- เอกสาร `/privacy` และ `/terms` เป็น**แม่แบบตั้งต้น** ควรให้ทนายตรวจก่อนเปิดใช้เชิงพาณิชย์จริง
- การรับเงินมัดจำแทนคู่สัญญาอาจเข้าข่ายธุรกิจบริการชำระเงินภายใต้การกำกับของ ธปท.
  ควรปรึกษาที่ปรึกษากฎหมายก่อนเปิดรับเงินจริงในวงกว้าง
- สัญญาเช่าที่เกิน 3 ปีต้องจดทะเบียนต่อพนักงานเจ้าหน้าที่จึงจะสมบูรณ์
- e-signature แบบพิมพ์ชื่อมีผลตาม พ.ร.บ.ธุรกรรมทางอิเล็กทรอนิกส์ แต่ความน่าเชื่อถือ
  ต่ำกว่าลายมือชื่อดิจิทัลที่มีใบรับรอง — ถ้าต้องการความรัดกุมสูงควรใช้ผู้ให้บริการ CA
