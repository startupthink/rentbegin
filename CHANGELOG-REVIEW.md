# รอบ Review — แก้ทุกฟังก์ชันที่ใช้การไม่ได้

สรุปสิ่งที่แก้ทั้งหมด · ทดสอบผ่าน 48/48 · build ผ่าน

---

## ⚠️ ต้องทำก่อนใช้งาน

รัน SQL 1 ไฟล์ใหม่ที่ Supabase → SQL Editor:
**`supabase/upgrade-listings.sql`** (เพิ่มฟิลด์ขาย/ห้อง + สิทธิ์แก้ไข-ลบ)

แล้ว push:
```bash
cd ~/Desktop/rentbegin
git add -A
git commit -m "fix: แก้ไข/ลบประกาศ, lightbox, ค้นหา, โหมดขาย, responsive + ชุดทดสอบ"
git push
```

---

## 1. เจ้าของแก้ไข/ลบประกาศได้แล้ว

**เดิม:** ลงประกาศไปแล้วแก้อะไรไม่ได้เลย ปุ่ม ✏️ กดไม่ติด

**ตอนนี้** — แดชบอร์ด → ประกาศของฉัน แต่ละรายการมี 4 ปุ่ม:

| ปุ่ม | ทำอะไร |
|---|---|
| ✏️ | เปิดฟอร์มพร้อมข้อมูลเดิม แก้ราคา รูป รายละเอียด ได้ทุกช่อง |
| 👁️ | เปิดหน้าประกาศจริง |
| ✅ / ↩︎ | ทำเครื่องหมายว่าปล่อยเช่าแล้ว / เปิดขายใหม่ |
| 🗑️ | ลบถาวร — **ต้องกดยืนยันอีกครั้ง** และลบรูปใน Storage ให้ด้วย |

## 2. ดูรูปเต็มจอได้แล้ว

คลิกรูปใดก็ได้ในหน้าประกาศ → เปิดเต็มจอ
- ลูกศร ‹ › หรือปุ่มคีย์บอร์ด ← →
- **ปัดนิ้วซ้าย-ขวาบนมือถือ**
- แถบ thumbnail ด้านล่างกดข้ามได้
- ESC หรือคลิกพื้นหลังเพื่อปิด

## 3. ปุ่มค้นหาใช้ได้จริงทั้งหมด

**เดิม:** "ทุกทำเล / ทุกประเภท / งบเท่าไหร่" บน header กดไม่ติด

**ตอนนี้:**
- กดแล้วกางแผงค้นหา กรอกแล้วกดค้นหา → ไปหน้าผลลัพธ์
- ชิปบนแถบหมวด (ติดรถไฟฟ้า, เฟอร์ครบ, เลี้ยงสัตว์ได้, มีสระ, ที่จอดรถ, ฟิตเนส) กรองได้จริง
- ปุ่ม **⚙ ตัวกรอง** เปิดแผง: เช่า/ขาย · ช่วงราคา 6 ระดับ · ห้องนอน · สิ่งอำนวยความสะดวกทั้งหมด
- แสดงตัวกรองที่ใช้อยู่เป็นแท็ก กด ✕ เอาออกทีละอัน
- **ตัวกรองอยู่ใน URL** → แชร์ลิงก์ผลค้นหาได้ กด back ได้

## 4. ปุ่มตายที่แก้ทั้งหมด

| ที่ไหน | ปุ่ม | ตอนนี้ทำอะไร |
|---|---|---|
| Header | ช่องค้นหา | กางแผงค้นหา |
| Header | ไอคอนบัญชี | เมนู: แดชบอร์ด / ที่บันทึก / แอดมิน / นโยบาย / ออกจากระบบ |
| หน้าประกาศ | ↗ แชร์ | Web Share บนมือถือ · คัดลอกลิงก์บนเดสก์ท็อป |
| หน้าประกาศ | 💬 ทักแชตเจ้าของ | โทรออก / เปิด LINE / พาไปล็อกอิน |
| แดชบอร์ด | ⬇ ดาวน์โหลด PDF | เปิดหน้าใบเสร็จให้พิมพ์หรือบันทึก PDF |
| แดชบอร์ด | ตั้งค่า → บันทึก | บันทึกชื่อ/เบอร์ลงฐานข้อมูลจริง |
| แถบซ้าย | แก้ไขเงื่อนไข | เปลี่ยนเป็นสรุปสถานะ + ลิงก์ค้นหา |
| แอดมิน | ↓ ส่งออกรายงาน | ดาวน์โหลด CSV เปิดใน Excel ได้ |
| แอดมิน | ดูหลักฐาน | กางรายละเอียดเคสข้อพิพาท |
| แอดมิน | โหมดตรวจประกาศ | สลับได้จริง |

**Route ครบทุกเส้น** — ตรวจแล้วทุกลิงก์ในโค้ดมีปลายทางจริง (`/`, `/property/:id`, `/saved`, `/login`, `/privacy`, `/terms`, `/member`, `/admin`) และ URL มั่วเด้งกลับหน้าแรก

## 5. ตัวเลือกห้องเพิ่มใหม่ 12 อย่าง

ห้องนั่งเล่น · ห้องครัว · ห้องอาหาร · ระเบียง · ห้องทำงาน · พื้นที่ซักล้าง · ห้องเก็บของ · ห้องแม่บ้าน · สวน · โรงจอดรถในตัว · อ่างอาบน้ำ · เครื่องทำน้ำอุ่น

(ห้องนอน/ห้องน้ำยังเป็นช่องกรอกตัวเลขเหมือนเดิม เพราะต้องระบุจำนวน)

## 6. โหมดขาย

ตอนลงประกาศเลือกได้ 3 แบบ: **ให้เช่า · ขาย · เช่าหรือขาย**
- เลือกขาย → มีช่องราคาขาย, ซ่อนช่องมัดจำ/สัญญา
- การ์ดแสดงป้าย 🏷️ ขาย / 🤝 เช่า-ขาย และราคาที่ถูกต้อง
- หน้าประกาศซ่อนปุ่ม "จองและวางมัดจำ" ถ้าขายอย่างเดียว
- กรองเฉพาะเช่าหรือเฉพาะขายได้ในแผงตัวกรอง

## 7. Responsive ทุกอุปกรณ์

**เดิม:** `viewport width=1280` บังคับให้มือถือเห็นเดสก์ท็อปย่อส่วน

**ตอนนี้** — `width=device-width` จริง พร้อม breakpoint ทุกหน้า:
- **≤1200px** — การ์ด 3 คอลัมน์
- **≤1000px** — ซ่อนแถบซ้าย, การ์ด 2 คอลัมน์, **dashboard มีเมนูแถบเลื่อนแนวนอน** (เดิมมือถือสลับแท็บไม่ได้เลย)
- **≤700px** — การ์ด 1 คอลัมน์, ฟอร์มค้นหาเรียงแนวตั้ง, ตาราง/แชต/ปุ่มปรับขนาด
- **≤420px** — ปรับปุ่มบน header ให้พอดีจอเล็ก

## 8. บั๊กอื่นที่เจอระหว่างตรวจ

- **หน้า "ที่บันทึกไว้" ใช้ไม่ได้** — กรองจากข้อมูลตัวอย่างที่ลบไปแล้ว ทำให้บันทึกประกาศจริงแล้วไม่ขึ้น → แก้เป็นดึงจาก Supabase จริง
- **แดชบอร์ดหมุนค้างถาวร** ถ้าโหลดโปรไฟล์ไม่สำเร็จ → แสดงข้อความ + ปุ่มลองใหม่แทน
- **จอขาว** ถ้าประกาศไม่มีรูป/ไม่มีรายละเอียด/ราคาว่าง → ใส่ fallback ทุกจุด
- รูปจริงไม่ขึ้นในแผงแอดมินและแถบซ้าย → แก้ให้ใช้ตัวช่วยเดียวกันทั้งระบบ

## 9. ชุดทดสอบอัตโนมัติ

เพิ่ม `npm test` — **48 เทสต์ ผ่านหมด**

```
tests/smoke.test.jsx      ทุกหน้าเรนเดอร์ได้ · route · ตัวกรอง       (10)
tests/logic.test.js       คำนวณยอดจอง · ตัวช่วยรูป · ค่าคงที่        (13)
tests/lightbox.test.jsx   เลื่อนรูป · คีย์บอร์ด · ปิด · ไม่มีรูป       (10)
tests/dashboard.test.jsx  แก้ไข/ลบ/เปลี่ยนสถานะ · โหมดขาย · validate  (10)
tests/responsive.test.js  viewport · breakpoint · ไม่มี mock หลงเหลือ  (5)
```

รันเองได้ทุกเมื่อ: `npm test`

---

## ยังไม่ได้ทำ (ตามที่ตกลง)

- **ชำระเงินจริง** — ต้องมี Omise merchant account (ดู `SETUP-PAYMENTS.md`)
- แผนที่จริง (ตอนนี้เป็นภาพประกอบ)
- แชตเรียลไทม์ (ตอนนี้ปุ่มพาไปโทร/LINE แทน)
